spring.application.name= event-management-app

# MariaDB database connection settings
spring.datasource.url=jdbc:mariadb://127.0.0.1:3306/event_db
spring.datasource.username=michelle
spring.datasource.password=HN9gGhk3fCSyyKN
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.sql.init.mode=always

# JPA/Hibernate settings
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Connection pool settings
spring.datasource.hikari.maximum-pool-size=10

# spring-session-jdbc config
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
server.servlet.session.cookie.name=SESSION

# file size upload
spring.servlet.multipart.max-file-size=4MB
spring.servlet.multipart.max-request-size=4MB

# Enable Batch Processing
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true

# disable whitelabel
server.error.whitelabel.enabled=false
